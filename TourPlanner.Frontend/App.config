<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <!-- API Configuration - Be sure this matches your backend URL -->
    <add key="ApiBaseUrl" value="https://localhost:7022/api" />
    
    <!-- Database Configuration - Reference for the backend, not used directly by frontend -->
    <add key="ConnectionString" value="Host=localhost;Database=swen2;Username=postgres;Password=postgres;Include Error Detail=true;" />
  </appSettings>
</configuration> 